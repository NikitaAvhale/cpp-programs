#include<iostream.h>
#include<conio.h>
void main()
{
 int n,i,j,sum,tsum;
 clrscr();
 cout<<"enter a number:";
 cin>>n;
 for(i=1;i<=n;i++)
 {
  tsum=0;
  for(j=1;j<=i;j++)
  {
   sum+=j;
   tsum+=j;
   cout<<j;
   {
    if(j<i)
    {
    cout<<"+";
    }
   }
  cout<<"="<<tsum;
  cout<<"\n";
 }
cout<<"The sum of all series: "<<sum;
}
getch();
}